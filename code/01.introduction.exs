# 01. Introduction to Elixir

# IO stands for Input/Output. 
# .puts is a function that "puts" a "string" (text) on the screen.

IO.puts("Hello from the BEAM City!")
IO.puts("In this city, every task gets its own mailman.")
